Da dire per giustificare la hold come attore: 

"Non comunicare condividendo la memoria. Piuttosto, condividi memoria comunicando"



Si richiede che ogni sprint effettui uno "zooming" su un macro-componente specifico senza perdersi in dettagli prematuri. Noi dichiariamo subito che il core business è il ciclo di gestione delle richieste di carico orchestrato dal cargoservice, ignorando per ora i dettagli implementativi di rete, database o hardware fisico.

2. L'uso dei Mock in questa fase è per avere un modello che sia eseguibile e testabile fin da subito, l'utilizzo di collaboratori simulati (ioportmock, sonarmock, ledmock, cargorobotmock) è la strada perfetta. Questo ti permette di testare la logica del tuo orchestratore in totale isolamento.


3. nello Sprint 0 è emerso chiaramente che il sistema non deve accodare le richieste se è già occupato, ma deve respingerle immediatamente. La architettura basata sugli stati (disengaged, handle_load_request, engaged) e sui controlli interni if [# !ServiceWorking || IOPortOccupied #] garantisce esattamente questo comportamento in pieno stile Macchina di Moore.
Come procedere per finire il documento:

4.ora va mostrato come i test funzionali definiti nello Sprint 0 (T1, T2, T3, T4, T5) vengono tradotti in test automatizzati sul tuo codice QAK
Project: Qui l'architettura logica finale catta da gabriele
    8. Deployment: poche righe su come lanciare il prototipo (es. tramite ./gradlew run o eseguendo un Main generato da QAK). Non serve ancora docker







==== chiaramente

alla fine di ogni sprint deve esserci un src con un qak da sottoporre alla macchina,

giustificare perchè LED e IOPORT sono nello stesso context, 

NON STIAMO modellando requisit, stiamo facendo un mix tra requisiti / pensieri su come 
sistemare sw che svilupperemo nello sprin1

Problema: IOPort in un context vs. LED come elemento di picow

se facciamo lo sforzo di definire alla fine dello sprint0 un modello e facciamo lo sforzo di sottoporlo alla macchina, ci troviamo gia una struttura che sarà l'input dello sprint1, ogni sprint comincia sempre col modello dello sprint precedente



sprint0 togli Project

cosa importante: abbiamo detto chde un software va nel picow, quindi nodo diverso, quindi context differente, basta che le entità software in un contesto non hanno memoria comune con altri 

differenza sprin0 sprin1

sprint0: analisi dei requisiti ESEGUIBILE


natura distribuita del problema : entità logiche che non condividono memoria

all'inizio è un delirio fare 4 contesti diversi, facciamo comunicar gli ATTORI senza memoria condivisa

i 4 contesti sono un requisito: SERVONO, 
nello sprint1 possiamo (fase prototipale) rinunciarci e mettere tutto in uno stesso contesto
alla fine sappiamo che dovrà girare in 4 contesti


